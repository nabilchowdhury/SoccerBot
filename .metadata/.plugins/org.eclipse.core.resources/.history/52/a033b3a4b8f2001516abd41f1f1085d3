package soccerbot;

public class TestClass {
	public static void main(String[] args){
		double theta = -8.345454;
		
		// Bounded theta
		if (theta > (Math.PI * 2)){
			theta %= (Math.PI * 2);
		}
		if (theta < 0){
			theta += (Math.PI * 2);
		}
		
		System.out.println(theta);
	}
}
